---
title: "Week 3 Assignment"
format: html
---

Exercises 1, 2, 3 are required for the badge.

1. In the `data/` folder, there is a list of `.csv` files. Modify the code to process each file.

```bash
#!/bin/bash
for file in ------
do
  head ------
done
```

2. Write a `for` loop in a script to count the number of lines for each file. The output should be:

```
echo "${file}\t(wc -l $file)\n"
```

Run the script and redirect your output to `files_out.txt`. Put your bash command you used below


```
```

3. Modify the below script to process the files in `data/week3/fastq/`

```bash
#!/bin/bash
#SBATCH --array=1-________  
ml 
```

4. Challenge: 